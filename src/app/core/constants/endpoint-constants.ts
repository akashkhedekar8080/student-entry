import { ApiEndPoints } from "./api-endpoints";

export const EndpointConstants = {
  STUDENT_RECORDS: ApiEndPoints.student.records,
  LOGIN: ApiEndPoints.auth.login,
  LOGOUT: ApiEndPoints.auth.logout,
};
